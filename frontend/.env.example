# Optional. Leave unset in development: Vite proxies "/api" to the backend
# on http://localhost:5000 (see vite.config.js), same as the Main frontend.
# Set this only if the API is hosted somewhere else, e.g. https://api.example.com/api
# VITE_API_URL=/api
